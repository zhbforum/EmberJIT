fn hot_loop(value: i64) -> i64 {
    return value;
}

fn main() -> void {
    let index: i64 = 0;
    let sum: i64 = 0;

    while index < 10000 {
        sum = sum + hot_loop(index);
        index = index + 1;
    }

    print_i64(sum);
    return;
}
